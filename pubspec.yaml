name: catordog_detector
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  bloc: ^7.2.1
  cupertino_icons: ^1.0.2
  equatable: ^2.0.3
  flutter:
    sdk: flutter
  flutter_bloc: ^7.3.0
  flutter_native_splash: ^1.2.4
  image_picker: ^0.8.3
  path_provider: ^2.0.5
  tflite: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^0.1.4
  bloc_test: ^8.2.0
  mockito: ^5.0.7

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/images/
